@file:Suppress("unused")

package io.github.ReadyMadeProgrammer.Spikot.chat

import org.bukkit.ChatColor.*

fun black(builder: Builder): ChatBuilder {
    return chat { black(builder) }
}

fun black(text: String): ChatBuilder {
    return chat { black(text) }
}

fun ChatBuilder.black(builder: Builder) {
    color(BLACK, builder)
}

fun ChatBuilder.black(text: String) {
    color(BLACK, text)
}

fun darkBlue(builder: Builder): ChatBuilder {
    return chat { darkBlue(builder) }
}

fun darkBlue(text: String): ChatBuilder {
    return chat { darkBlue(text) }
}

fun ChatBuilder.darkBlue(builder: Builder) {
    color(DARK_BLUE, builder)
}

fun ChatBuilder.darkBlue(text: String) {
    color(DARK_BLUE, text)
}

fun darkGreen(builder: Builder): ChatBuilder {
    return chat { darkGreen(builder) }
}

fun darkGreen(text: String): ChatBuilder {
    return chat { darkGreen(text) }
}

fun ChatBuilder.darkGreen(builder: Builder) {
    color(DARK_GREEN, builder)
}

fun ChatBuilder.darkGreen(text: String) {
    color(DARK_GREEN, text)
}

fun darkAqua(builder: Builder): ChatBuilder {
    return chat { darkAqua(builder) }
}

fun darkAqua(text: String): ChatBuilder {
    return chat { darkAqua(text) }
}

fun ChatBuilder.darkAqua(builder: Builder) {
    color(DARK_AQUA, builder)
}

fun ChatBuilder.darkAqua(text: String) {
    color(DARK_AQUA, text)
}

fun darkRed(builder: Builder): ChatBuilder {
    return chat { darkRed(builder) }
}

fun darkRed(text: String): ChatBuilder {
    return chat { darkRed(text) }
}

fun ChatBuilder.darkRed(builder: Builder) {
    color(DARK_RED, builder)
}

fun ChatBuilder.darkRed(text: String) {
    color(DARK_RED, text)
}

fun darkPurple(builder: Builder): ChatBuilder {
    return chat { darkPurple(builder) }
}

fun darkPurple(text: String): ChatBuilder {
    return chat { darkPurple(text) }
}

fun ChatBuilder.darkPurple(builder: Builder) {
    color(DARK_PURPLE, builder)
}

fun ChatBuilder.darkPurple(text: String) {
    color(DARK_PURPLE, text)
}

fun gold(builder: Builder): ChatBuilder {
    return chat { gold(builder) }
}

fun gold(text: String): ChatBuilder {
    return chat { gold(text) }
}

fun ChatBuilder.gold(builder: Builder) {
    color(GOLD, builder)
}

fun ChatBuilder.gold(text: String) {
    color(GOLD, text)
}

fun gray(builder: Builder): ChatBuilder {
    return chat { gray(builder) }
}

fun gray(text: String): ChatBuilder {
    return chat { gray(text) }
}

fun ChatBuilder.gray(builder: Builder) {
    color(GRAY, builder)
}

fun ChatBuilder.gray(text: String) {
    color(GRAY, text)
}

fun darkGray(builder: Builder): ChatBuilder {
    return chat { darkGray(builder) }
}

fun darkGray(text: String): ChatBuilder {
    return chat { darkGray(text) }
}

fun ChatBuilder.darkGray(builder: Builder) {
    color(DARK_GRAY, builder)
}

fun ChatBuilder.darkGray(text: String) {
    color(DARK_GRAY, text)
}

fun blue(builder: Builder): ChatBuilder {
    return chat { blue(builder) }
}

fun blue(text: String): ChatBuilder {
    return chat { blue(text) }
}

fun ChatBuilder.blue(builder: Builder) {
    color(BLUE, builder)
}

fun ChatBuilder.blue(text: String) {
    color(BLUE, text)
}

fun green(builder: Builder): ChatBuilder {
    return chat { green(builder) }
}

fun green(text: String): ChatBuilder {
    return chat { green(text) }
}

fun ChatBuilder.green(builder: Builder) {
    color(GREEN, builder)
}

fun ChatBuilder.green(text: String) {
    color(GREEN, text)
}

fun aqua(builder: Builder): ChatBuilder {
    return chat { aqua(builder) }
}

fun aqua(text: String): ChatBuilder {
    return chat { aqua(text) }
}

fun ChatBuilder.aqua(builder: Builder) {
    color(AQUA, builder)
}

fun ChatBuilder.aqua(text: String) {
    color(AQUA, text)
}

fun red(builder: Builder): ChatBuilder {
    return chat { red(builder) }
}

fun red(text: String): ChatBuilder {
    return chat { red(text) }
}

fun ChatBuilder.red(builder: Builder) {
    color(RED, builder)
}

fun ChatBuilder.red(text: String) {
    color(RED, text)
}

fun lightPurple(builder: Builder): ChatBuilder {
    return chat { lightPurple(builder) }
}

fun lightPurple(text: String): ChatBuilder {
    return chat { lightPurple(text) }
}

fun ChatBuilder.lightPurple(builder: Builder) {
    color(LIGHT_PURPLE, builder)
}

fun ChatBuilder.lightPurple(text: String) {
    color(LIGHT_PURPLE, text)
}

fun yellow(builder: Builder): ChatBuilder {
    return chat { yellow(builder) }
}

fun yellow(text: String): ChatBuilder {
    return chat { yellow(text) }
}

fun ChatBuilder.yellow(builder: Builder) {
    color(YELLOW, builder)
}

fun ChatBuilder.yellow(text: String) {
    color(YELLOW, text)
}

fun white(builder: Builder): ChatBuilder {
    return chat { white(builder) }
}

fun white(text: String): ChatBuilder {
    return chat { white(text) }
}

fun ChatBuilder.white(builder: Builder) {
    color(WHITE, builder)
}

fun ChatBuilder.white(text: String) {
    color(WHITE, text)
}